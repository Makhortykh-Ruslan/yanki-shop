export interface CustomerInterface {
  firstName: string;
  lastName: string;
  login: string;
  email: string;
  password: string;
  telephone: string;
  gender?: string;
  avatarUrl?: string;
  isAdmin?: boolean;
}
